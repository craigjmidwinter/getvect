# dependencies & build output
node_modules/
dist/
.vite/

# generated, fully reproducible from the scripts (see docs/HARNESS.md)
artifacts/
# The local fixture overlay: assets we may MEASURE against but may never
# redistribute. Nothing under it is ever tracked — not a .gitkeep, not the
# manifest. Its absence must change nothing (docs/HARNESS.md "Local fixtures").
fixtures/local/
test-results/
playwright-report/
nanobanana-output/

# noise
*.log
.DS_Store
Thumbs.db
.env
.env.*
!.env.example
