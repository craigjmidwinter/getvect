/** Injected by vite.web.config.mts from package.json, so the About text and the
 *  bridge report the same version the repo is at without a runtime read. */
declare const __GETVECT_VERSION__: string;
